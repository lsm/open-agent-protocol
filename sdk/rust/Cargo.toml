[package]
name = "oap-sdk"
version = "0.1.0"
edition = "2021"
rust-version = "1.82"
description = "Rust SDK for the Open Agent Protocol project's runtime, spoken over the `oapx --stdio` protocol"
repository = "https://github.com/lsm/open-agent-protocol"
license = "MIT"
readme = "README.md"
keywords = ["ai", "llm", "streaming", "agent", "sdk"]
categories = ["api-bindings", "asynchronous"]

[features]
default = []
# Enables downloading the runtime binary from `OAP_SDK_BINARY_URL` / `binary_url`.
# Without it a configured URL is only usable when the checksum-verified file is
# already present in the cache directory.
download = ["dep:reqwest"]

[dependencies]
async-stream = "0.3"
futures-core = "0.3"
rand = "0.9"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
thiserror = "2"
tokio = { version = "1", features = [
    "fs",
    "io-util",
    "macros",
    "process",
    "rt",
    "sync",
    "time",
] }
tracing = "0.1"

[dependencies.reqwest]
version = "0.12"
optional = true
default-features = false
features = ["rustls-tls"]

[dev-dependencies]
futures = "0.3"
tempfile = "3"
tokio = { version = "1", features = ["rt-multi-thread", "test-util"] }

# Protocol fake used by the integration tests. It is a standalone `oapx --stdio`
# look-alike, so downstream crates can point `MakaiClient` at it to test their own
# code without a real runtime or provider credentials.
[[bin]]
name = "makai-protocol-fake"
path = "src/bin/protocol_fake.rs"

[[bin]]
name = "oap-protocol-fake"
path = "src/bin/oap_protocol_fake.rs"

[lints.rust]
unsafe_code = "forbid"
