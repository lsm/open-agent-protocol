# V1 Implementation Traceability Matrix

Status: active

## How to use

1. Each row maps one normative requirement to code and tests.
2. `Clause` must reference exact section IDs from spec/design/plan docs.
3. `Status` values: `not started`, `in progress`, `done`.
4. Every implementation PR must update rows it touches.

## Phase Mapping (Plan -> Spec Rollout)

This matrix is keyed by integration-plan phases and explicitly mapped to spec rollout phases.

| Integration Plan Phase | Spec Rollout Phase(s) | Notes |
|---|---|---|
| Phase 1 | prerequisite | Transport/runtime wiring prerequisite for all protocol surfaces. |
| Phase 1.25 | Phase A | Auth protocol runtime first (`auth_providers_request`, `auth_login_start`, auth event loop). |
| Phase 1.5 | Phase D, Phase E | Provider model discovery + agent model passthrough. |
| Phase 2a | cross-cutting prerequisite | Credential resolution in binary request path. |
| Phase 2b | cross-cutting (`§8` error/auth semantics) | Refresh + retry behavior and typed failures. |
| Phase 2c | cross-cutting hardening | Concurrency locks + persistence race safety. |
| Phase 3 | Phase B, Phase D | TS SDK surfaces (auth/models/provider/agent). |
| Phase 4 | Phase F | Demo migration to spec interfaces only. |
| Phase 5 | Phase C | CLI auth wrappers over auth protocol runtime. |
| Phase 6 | acceptance/hardening | End-to-end test and stability gate across all phases. |

## Matrix

| Row ID | Phase | Clause | Requirement Summary | Code Paths | Tests | Issue | PR | Status |
|---|---|---|---|---|---|---|---|---|
| M-001 | 1 | `DESIGN.md §3`; `docs/ts-sdk-chat-integration-plan.md` Phase 1 | Wire protocol runtimes under `makai --stdio` with ready handshake compatibility. | `zig/src/tools/makai.zig` (`StdioProtocolLoop`, `runStdioMode`, `main --stdio`); `zig/build.zig` (`makai_cli` protocol imports + `test-unit-makai-cli` target) | `zig/src/tools/makai.zig` tests: `stdio protocol loop decodes and dispatches provider and agent envelopes`; `stdio protocol loop forwards provider event result and error envelopes`; `stdio mode preserves ready handshake compatibility`; command: `zig build test-unit-makai-cli` | `n/a (no repo issues listed as of 2026-02-23)` | [#16](https://github.com/lsm/makai/pull/16) | done |
| M-002 | 1.25 | `docs/v1-sdk-agent-provider-spec.md §4`; `DESIGN.md §3`; `docs/ts-sdk-chat-integration-plan.md` Phase 1.25 | Implement auth protocol runtime (`types`, `envelope`, `server`, `runtime`) and stdio integration. | `zig/src/protocol/auth/types.zig`; `zig/src/protocol/auth/envelope.zig`; `zig/src/protocol/auth/server.zig`; `zig/src/protocol/auth/runtime.zig`; `zig/src/tools/makai.zig` (`StdioProtocolLoop` auth dispatch + background pump); `zig/build.zig` (auth protocol module/test wiring); `zig/src/utils/oauth/storage.zig` (safe provider-id key ownership on load). | `zig/src/tools/makai.zig` tests: `stdio protocol loop decodes and dispatches auth providers request and emits ack then response`; `stdio auth login flow supports prompt loop terminal ordering and no secret leakage`; `stdio auth login flow cancellation emits cancelled result and ignores late prompt responses`; `stdio auth login failure emits auth_event.error before auth_login_result`; command: `zig build test-unit-makai-cli`; command: `zig build test-unit-protocol` | `n/a (no repo issues listed as of 2026-02-23)` | [#17](https://github.com/lsm/makai/pull/17) | done |
| M-003 | 1.5 | `docs/v1-sdk-agent-provider-spec.md §3.1`; `docs/ts-sdk-chat-integration-plan.md` Phase 1.5 | Implement `model_ref` parse/format helpers (`protocol/model_ref.zig`) and validation tests; TS diagnostic parser utility. | `zig/src/protocol/model_ref.zig`; `zig/build.zig` (model_ref module + `test-unit-protocol` wiring); `typescript/src/diagnostics/model_ref.ts`; `typescript/test/model_ref_diagnostics.test.ts` | `zig/src/protocol/model_ref.zig` tests: `model_ref format and parse roundtrip canonical refs`; `model_ref supports colons and reserved chars via percent encoding`; `model_ref supports UTF-8 model ids via percent encoding`; `model_ref parse rejects malformed refs`; command: `cd zig && zig test src/protocol/model_ref.zig`; command: `cd zig && zig build test-unit-protocol`; `typescript/test/model_ref_diagnostics.test.ts`: `parseModelRef parses canonical refs with reserved chars and UTF-8 model IDs`; `parseModelRef rejects malformed refs with Zig-matching error categories`; command: `node --test dist/test/model_ref_diagnostics.test.js`; command: `npm test` | [#20](https://github.com/lsm/makai/issues/20) | [#21](https://github.com/lsm/makai/pull/21) | done |
| M-004 | 1.5 | `docs/v1-sdk-agent-provider-spec.md §2.3`, `§5`; `docs/ts-sdk-chat-integration-plan.md` Phase 1.5 | Implement provider model catalog sources, caching metadata, and `models_request/models_response`. | `zig/src/protocol/model_catalog_types.zig`; `zig/src/protocol/provider/types.zig`; `zig/src/protocol/provider/envelope.zig`; `zig/src/protocol/provider/server.zig`; `zig/src/protocol/provider/runtime.zig`; `zig/src/tools/makai.zig`; `zig/build.zig` | `zig/src/protocol/model_catalog_types.zig` tests: `cloneModelDescriptor duplicates nested owned fields`; `zig/src/protocol/provider/types.zig` tests: `ModelsRequest getters return null for empty borrowed filters`, `ModelsRequest deinit frees owned filter strings`; `zig/src/protocol/provider/envelope.zig` tests: `serializeEnvelope and deserializeEnvelope roundtrip with models_request`, `serializeEnvelope and deserializeEnvelope roundtrip with models_response`; `zig/src/protocol/provider/server.zig` tests: `handleModelsRequest emits ack then models_response from static fallback`, `handleModelsRequest returns not_implemented nack when unsupported`, `handleModelsRequest applies provider api and exact model filters`, `handleModelsRequest returns invalid_request for ambiguous or missing model_id`, `handleModelsRequest prefers dynamic fetch and falls back to static catalog when unavailable`; command: `cd zig && zig build test-protocol-types`; command: `cd zig && zig build test-unit-makai-cli`; command: `cd zig && zig build test-unit-protocol`; command: `npm test` | [#22](https://github.com/lsm/makai/issues/22) | [#23](https://github.com/lsm/makai/pull/23) | done |
| M-005 | 1.5 | `docs/v1-sdk-agent-provider-spec.md §6`; `docs/ts-sdk-chat-integration-plan.md` Phase 1.5 | Implement agent passthrough model discovery with shared response shape. | TBD | TBD | TBD | TBD | not started |
| M-006 | 2a | `docs/ts-sdk-chat-integration-plan.md` Phase 2a | Implement credential resolution/load path in binary request handling. | `zig/src/utils/auth_resolver.zig` (new); `zig/src/protocol/provider/types.zig` (`auth_required`/`auth_refresh_failed`/`auth_expired` codes); `zig/src/protocol/provider/envelope.zig` (parse new codes); `zig/src/protocol/provider/server.zig` (`auth_storage` option, `resolveAndInjectCredentials`, stream + complete request paths); `zig/src/protocol/agent/types.zig` (`auth_required` propagation); `zig/build.zig` (`auth_resolver` module wiring) | `zig/src/utils/auth_resolver.zig` tests: `resolveApiKey - explicit api key wins, no storage lookup`, `resolveApiKey - empty explicit key falls through to storage`, `resolveApiKey - loads api_key from storage by provider_id`, `resolveApiKey - loads oauth access token from storage by provider_id`, `resolveApiKey - missing storage and no key returns AuthRequired`, `resolveApiKey - empty storage returns AuthRequired`, `resolveApiKey - provider not in storage returns AuthRequired`, `resolveApiKey - empty key with no storage returns AuthRequired`; `zig/src/protocol/provider/server.zig` tests: `credential resolution: explicit api_key bypasses storage lookup`, `credential resolution: missing api_key loads credentials from storage by provider_id`, `credential resolution: missing api_key and missing storage entry returns auth_required nack`, `credential resolution: complete_request without credentials returns auth_required nack`; commands: `zig build test-unit-protocol`, `zig build test-unit-makai-cli`, `zig build test-unit-utils` | TBD | TBD | in progress |
| M-007 | 2b | `docs/v1-sdk-agent-provider-spec.md §8`; `docs/ts-sdk-chat-integration-plan.md` Phase 2b | Implement refresh, retry-once behavior, and typed `auth_refresh_failed`/`auth_expired` handling. | TBD | TBD | TBD | TBD | not started |
| M-008 | 2c | `docs/ts-sdk-chat-integration-plan.md` Phase 2c | Implement refresh lock scope/timeouts and race-safe persistence. | TBD | TBD | TBD | TBD | not started |
| M-009a | 3 | `docs/v1-sdk-agent-provider-spec.md §3.6`; `docs/ts-sdk-chat-integration-plan.md` Phase 3 | Implement TS `client.auth.*` API over protocol transport. | TBD | TBD | TBD | TBD | not started |
| M-009b | 3 | `docs/v1-sdk-agent-provider-spec.md §3.5`; `docs/ts-sdk-chat-integration-plan.md` Phase 3 | Implement TS `client.models.*` API including resolve mapping behavior. | TBD | TBD | TBD | TBD | not started |
| M-009c | 3 | `docs/v1-sdk-agent-provider-spec.md §3`; `docs/ts-sdk-chat-integration-plan.md` Phase 3 | Implement TS `client.provider.*` and `client.agent.*` APIs. | `typescript/src/execution_client.ts` (`parseCompletionResponse`, `buildCompletionResponseFromMessage`, `client.agent.run` parsing path); `typescript/test/execution_client.test.ts` (completion response shape/validation tests) | `typescript/test/execution_client.test.ts`: `client.provider.complete surfaces error_message from error results`; `client.agent.run surfaces error_message from error agent results`; `client.provider.complete resolves with correct CompletionResponse shape`; `client.agent.run resolves with correct AgentRunResponse` | `n/a (no repo issue)` | [#158](https://github.com/lsm/makai/pull/158) | done |
| M-010 | 3 | `docs/v1-sdk-agent-provider-spec.md §3.4` | Implement and test stream lifecycle guarantees (terminal events, usage aggregation, error dedupe, tool-call buffering). | TBD | TBD | TBD | TBD | not started |
| M-011 | 3 | `docs/v1-sdk-agent-provider-spec.md §3.6`, `§4`; `docs/ts-sdk-chat-integration-plan.md` Phase 3 | Implement auth retry policy + handler precedence + auth event flattening behavior in SDK. | TBD | TBD | TBD | TBD | not started |
| M-012 | 4 | `docs/v1-sdk-agent-provider-spec.md §9` Phase F; `docs/ts-sdk-chat-integration-plan.md` Phase 4 | Migrate demo to SDK-backed provider-agnostic path (no direct auth-file/provider-specific HTTP logic). | TBD | TBD | TBD | TBD | not started |
| M-013 | 5 | `docs/v1-sdk-agent-provider-spec.md §9` Phase C; `docs/ts-sdk-chat-integration-plan.md` Phase 5 | Migrate `makai auth` CLI commands to protocol-wrapper mode with compatibility output. | `zig/src/tools/auth_cli.zig` (`runProvidersCommand`, `runLoginCommand`, `FileIo`, `TestIo`, `AuthCliIo` vtable); `zig/src/tools/makai.zig` (`handleAuth`, `handleAuthWithOptions`, `PRODUCTION_AUTH_SERVER_OPTIONS`, `AuthCliHarness`); `zig/build.zig` (`auth_cli` module + `test-unit-makai-cli` wiring) | `zig/src/tools/auth_cli.zig` tests: `runProvidersCommand emits plain-text provider list`, `runProvidersCommand --json emits backward-compatible providers payload`, `runLoginCommand drives prompt loop and emits success line`, `runLoginCommand --json emits structured event stream`, `runLoginCommand returns AuthLoginFailed for unknown provider`; `zig/src/tools/makai.zig` tests: `handleAuth providers end-to-end through CLI wrapper emits provider ids`, `handleAuth providers --json end-to-end emits backward-compatible shape`, `handleAuth login end-to-end drives prompt loop through CLI wrapper`, `handleAuth login surfaces typed error for unknown provider via CLI wrapper`; commands: `zig build test-unit-makai-cli`; `zig build test-unit-protocol` | `n/a (no repo issue)` | [#27](https://github.com/lsm/makai/pull/27) | in progress |
| M-014 | 6 | `docs/v1-sdk-agent-provider-spec.md §10`; `docs/ts-sdk-chat-integration-plan.md` Phase 6 | Hardening and acceptance tests across auth/provider/agent/CLI paths. | `typescript/src/execution_client.ts` (`createMakaiClient`, agent model passthrough facade); `typescript/demo/server.ts`; `typescript/test/fixtures/execution-server.js`; existing Zig auth/provider/agent/CLI runtime paths from M-001..M-013. | `typescript/test/execution_client.test.ts`: acceptance coverage for OAuth + model discovery + selected model execution, provider/agent identical model list shape, shared provider/agent `model_ref`; `typescript/test/demo_server.test.ts`: provider-agnostic SDK stream path assertions; `typescript/test/makai_binary_smoke.test.ts`: `MAKAI_BINARY_PATH`-gated binary auth/stdio/version-skew coverage; commands: `zig build test-unit-makai-cli`; `zig build test-unit-protocol`; `npm test` (real-binary e2e skipped unless `MAKAI_BINARY_PATH` is configured). | TBD | TBD | in progress |

## Out-of-Phase Protocol Surfaces

Rows here are normative protocol work that is **not** part of the V1 integration-plan
phases, so they are keyed to their own specification rather than to a phase in the
mapping table above. `docs/review-process.md` §2 scopes the heavier gate to V1
implementation PRs in Phases 1, 1.25, 1.5, 2a, 2b, 2c and 3 through 6; these rows are
tracked here anyway because they are protocol work and the clause/test discipline is
worth the same rigour.

| Row ID | Surface | Clause | Requirement Summary | Code Paths | Tests | Issue | PR | Status |
|---|---|---|---|---|---|---|---|---|
| M-OAP-001 | native Open Agent Protocol endpoint | `lsm/open-agent-protocol` `drafts/agent-control-core.md` ("Core Envelope", "Minimum Core Surface", "Core Data Shapes", "Core Capability Keys", "Capability Snapshot Freshness", "Minimum Conformance"); `drafts/conformance.md` ("Core Profile Requirements" 1-14, "Executable Validation And Fixture Plan" stateful checks 1-4 and 8-9); `decisions/0001-agent-control-v0.1-executable-core.md` (typed identity domains; one foreground invocation is one run; deterministic run event order; cancellation intent is not settlement); `decisions/0002-admission-before-start.md` ("Two canonical admission shapes" shape 1); `decisions/0005-run-controls.md` (fail-closed gate); `schema/v0.1/{envelope,common,session,run,capabilities}.schema.json`; `docs/oap-alignment.md` ("Native OAP mode") | Serve the agent-control-core profile natively over a JSONL-on-stdio binding: flat envelope codec, initialize/capabilities negotiation with an opaque `capability_revision` and `stale_capabilities` precondition, session open/state/state-updated, admitted submission with `auto -> start` resolution and one foreground run per session, generated contiguous per-run sequencing, streamed `content.delta`, exactly one terminal per accepted run under a terminal arbiter, cancellation as nonterminal intent plus authoritative settlement, the `+run-controls` fail-closed refusal discipline, and typed `error.response` for every unsupported or invalid request. | `zig/src/protocol/oap/types.zig`; `zig/src/protocol/oap/envelope.zig`; `zig/src/protocol/oap/server.zig`; `zig/src/protocol/oap/bridge.zig`; `zig/src/tools/makai.zig` (`runOapMode`, `parseOapModeArgs`, `pumpOapIntents`, `writeOapOutbound`, `main --oap`); `build.zig` (`protocol_oap_*` modules + `test`/`test-unit-protocol` wiring, `makai_cli_module` imports) | `zig/src/protocol/oap/types.zig`: payload/terminal/run-control vocabulary tests; `zig/src/protocol/oap/envelope.zig`: codec round-trips for submit/terminal/error/capabilities, protocol-version-profile rejection, malformed and unknown-type rejection, present-but-empty control preservation; `zig/src/protocol/oap/server.zig`: initialize negotiation and refusal, revisioned capabilities, bootstrap requests ignoring a stale revision, `stale_capabilities` on every other request, session open idempotence, admission shape and contiguous run sequence, one-run-per-session refusal, cancellation intent vs settlement, idempotent re-cancel, completion winning the cancel race, duplicate-terminal suppression, `run_already_terminal`, stale-cancel rejection, run-control refusal order and details, unsupported delivery mode, envelope/payload id disagreement, malformed line handling; `zig/src/protocol/oap/bridge.zig`: native `agent_start`/`agent_message` generation and sequencing, typed identity separation, text/thinking delta mapping, `agent_result` retained as evidence, settlement on `agent_end`, error and `agent_stopped` paths, unmapped-session and malformed-frame isolation, transport-failure settlement, and the three golden traces matching the externally validated ones; commands: `zig build test-unit-protocol`, `zig build test-unit-makai-cli`, `zig build test` | [lsm/open-agent-protocol#3](https://github.com/lsm/open-agent-protocol/issues/3) | this PR | in progress (core profile + `+run-controls`; tools, permissions, user input, persistence, models, queue/steer/btw, capability updates and extension packs not implemented) |

## Review Gate Tracking

| PR | External Round | Reviewer | Blocking Findings (P0/P1) | Resolved Commit | Status |
|---|---|---|---|---|---|
| TBD | TBD | TBD | TBD | TBD | open |
